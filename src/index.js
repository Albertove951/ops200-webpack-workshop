import React from 'react';
import ReactDom from 'react-dom';

const rootElement = document.getElementById('root');
const sampleComponent = 'React and webpack are working!';

ReactDOM.render(sampleComponent, rootElement);
